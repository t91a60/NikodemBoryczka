document.addEventListener("DOMContentLoaded",()=>{const username="t91a60";const repos=["osp-logbook","AlkoRater","PurrOS"];repos.forEach(repo=>{fetch(`https: .then(response=>{if(!response.ok){throw new Error(`Repo ${repo}not found or private.`);}return response.json();}).then(data=>{const starsElements=document.querySelectorAll(`.github-stars-${repo.toLowerCase()}`);const forksElements=document.querySelectorAll(`.github-forks-${repo.toLowerCase()}`);starsElements.forEach(el=>{el.innerHTML=`⭐ ${data.stargazers_count}`;el.classList.add("visible");});forksElements.forEach(el=>{if(data.forks_count>0){el.innerHTML=`🍴 ${data.forks_count}`;el.classList.add("visible");}});}).catch(error=>{console.log(`GitHub API note for ${repo}:`,error.message);});});});